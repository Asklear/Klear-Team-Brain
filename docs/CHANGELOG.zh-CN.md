# 更新日志

[English](./CHANGELOG.en.md) | 中文

本项目版本号遵循[语义化版本](https://semver.org/lang/zh-CN/)，格式参考 [Keep a Changelog](https://keepachangelog.com/zh-CN/1.1.0/)。本机版本可用 `brain version` 查看。

> 项目仍处于 1.0 前的早期阶段，按 patch 递增。版本管理在 `v0.1.11` 正式引入，更早的版本是按里程碑对历史开发的回溯切分。

---

## [0.1.22] - 2026-06-25 · 只读查询加固 + status 看得见采集足迹

### 新增
- **`brain status` 显示采集足迹、同步坏了会告警**：从结果账本展示「已传/跳过」足迹，并扫日志尾巴对失败/报错告警，同步坏了不再显得一切正常；用 launchctl 退出码区分「未装/未加载」与「已加载但没跑」，不再被正则兜成误导状态。
- **`/health` 改为真·就绪检查**：校验真相库存在、是 git 仓、可写；不就绪降级回 `503` + version，不再盲目 `200`。
- code-state 活跃分支窗口可配 `CODESTATE_ACTIVE_DAYS`（默认 30）。

### 改进
- `brain join` 首次回填改为滚动日志、结尾给「本机足迹」小结。
- 概览 / 仓库 / code-state 的「未推进度」措辞改为中性的「push 后有活动」。

### 修复
- **`grep` 非法正则回清晰报错**，不再吞成「无命中」。
- **`GITHUB_TOKEN_FILE` 配了却读不到 → 启动告警**，不再静默以 token-less 跑。
- **`/read` 大文件护栏**：超大整读拒绝；无 limit 时默认 5000 行截断带分页提示。
- `brain join` 可达探测与 `/health` `503` 解耦：任意 HTTP 响应即「连得上」。

## [0.1.21] - 2026-06-25 · 查看器打磨 + 日志英文化

### 修复
- 本机足迹查看器：侧边栏页脚固定满高（不再只在 overview 页显示），Activity 日志全部英文化。

## [0.1.20] - 2026-06-25 · 查看器免登录 + 设备 token

### 改进
- **本机足迹查看器不再要求 token 登录**（本机访问本机无需口令），改为直接展示设备 token；线上控制台新增「本机控制台」入口，一键跳到本机查看器。

## [0.1.19] - 2026-06-25 · 生产者本机足迹查看器

### 新增
- **生产者本机足迹查看器**：每个产出者都能打开本机网页，查看自己究竟有哪些 session 被采集、上传了哪些足迹，逐条排除或撤回已进记忆库的内容，并维护个人脱敏词表（自定义敏感词在上传前抹掉）——对「什么离开了你的机器」做自助式透明管控。装机包现已补带 `web/viewer.*`（此前真实安装会 404），且客户端零新增依赖。

## [0.1.18] - 2026-06-25 · 任意 agent 远程挂载（HTTP 传输 MCP）

### 新增
- **远程 MCP 端点（`POST /mcp`）**：任何支持 MCP 的 agent——队友的 Cursor、基于 Agent SDK / Codex SDK 的 IM 机器人、云端 agent——现在只需「URL + Bearer token」即可挂载团队记忆，无需安装客户端、无需 spawn 子进程。与编辑器 MCP 同样的 8 个只读原语，由服务端直接提供（无状态、JSON 响应），`/read` 出口的密钥/路径脱敏照旧。工具定义单源化，stdio 与 HTTP 两种传输不会漂移。

## [0.1.17] - 2026-06-24 · 本地尝鲜 + Docker 一键自托管

### 新增
- **本地 5 分钟尝鲜**：不用先搞服务器，`git clone` 后 `npm run quickstart` 就能在本机单人把整套跑起来（免 VPS、免 HTTPS、免邀请码），先试再决定要不要正经部署。
- **Docker 一键自托管**：`docker compose up -d` 起服务端，真相库自动持久化、token 自动签发；`--profile tls` 一条命令给真域名上自动 HTTPS。README 与部署文档同步加了上手步骤。

## [0.1.16] - 2026-06-24 · 客户端自动更新

### 新增
- **客户端自动更新**：常驻采集器每天自检一次，发现服务端有更新版客户端就自动更新、以新代码重启，不用再手动 `brain update`。默认开启，可在配置里关闭（`auto_update: false`）。
- **采集 Trae 会话记忆**：用 [Trae](https://www.trae.ai/) 干活产生的原生会话记忆也能进记忆库（默认开启，仍只采 `upload_folders` 范围内）。

### 改进
- **统计按「哪天干的算哪天」**：跨多天的 session 不再整条计到开始那天，而是按实际工作日拆分；按天分桶统一用北京时间（UTC+8）。

### 修复
- **修复重度 Codex 用户 token 统计为 0**：脱敏曾误伤 Codex 的数值型 token 计数；修复后 token 用量准确，并能按天精确拆分。升级后会自动从你本机原文补回历史。

## [0.1.15] - 2026-06-23 · 采集更省心、密钥不出网、客户端更轻

### 新增
- **安装默认全采**：`brain join` / `setup` 不指定工作空间时，默认采集本机所有 session（本地文档仍按 `upload_folders` 白名单）；纯消费者保持空白名单与之区分。

### 改进
- **上传前客户端脱敏**：密钥 / token / URL 内嵌口令在 session 离开你机器**之前**就被抹成占位符，记忆库的原始 `.jsonl` 本身不含密钥；完整原文留在产出者本机。规则取自 gitleaks 高置信子集。
- **装机包瘦身 59M → 24M**：客户端只装真正用到的依赖，砍掉仅服务端用的飞书 SDK；`brain update` 会自动清理旧残留。
- **超大 Codex 会话不再被丢弃**：采集改为流式逐行处理，几百 MB 的超大 rollout 也能正常入库（旧版会因超体积上限被整条跳过）。

### 修复
- 新增存量脱敏脚本，对历史 session 补抹密钥，确保升级前入库的内容也被覆盖。

## [0.1.14] - 2026-06-23 · 聚合统计

### 新增
- **统计看板**：新的只读查询 `stats`（MCP 工具 + Web「统计」页）。按 天 / 周 / 人 / repo / 工具 维度汇总 **token 用量 / session 数 / 对话轮次**，支持多维组合、正交细分、时间维倒序翻页。按真实工作时间聚合。

## [0.1.11] - 2026-06-18

### 新增
- `brain version`：查看客户端版本。
- 引入 `CHANGELOG`，并对历史开发回溯打 tag。

## [0.1.10] - 2026-06-18

### 新增
- 支持 **GitLab / Gitea** 代码托管（GitHub 不变、零迁移），可登记自建实例 / group / project。

## [0.1.9] - 2026-06-18

### 新增
- **Web 浏览界面**：静态前端看板，浏览全队 space / session / 文档 / 活动流，整站中英双语。
- Markdown 渲染补全、默认折叠噪声（大代码块 / 元数据 / 重复消息）、会话统计与分享。

## [0.1.8] - 2026-06-16

### 新增
- session 历史 markdown 同步。

### 修复
- MCP 客户端加超时 + 重试，扛跨境链路的 TLS 丢包。

## [0.1.7] - 2026-06-13

### 新增
- **开源发布**：LICENSE / 中英 README / CONTRIBUTING / SECURITY / 行为准则 / 部署文档 + 发布管线。
- CI（密钥扫描）+ issue / PR 模板。

## [0.1.6] - 2026-06-11

### 新增
- 新人上手指南；`upload_folders` 支持 `~` 展开。

### 改进
- 部署改为服务器从 GitHub main 拉取，确立 **main = 生产**。

## [0.1.5] - 2026-06-10

### 新增
- **飞书 / Lark 文档单向镜像**：wiki 正文增量拉进记忆库，用现有 `grep` / `read` 即可覆盖文档层，无需新工具。

## [0.1.4] - 2026-06-09

### 新增
- **session 蒸馏 v2**：按记录类型做减法（丢遥测 / 去重 / 截巨型 tool 输出与推理），根治超大 session 撑爆服务器；存量一次性迁移。
- 按人、按真实工作时间检索 session，修「某人上周做了什么」漏检。

### 修复
- Codex 会话取真实分支 / remote，修历史全被标成 main。
- 单文件出错不再拖垮整轮采集。

## [0.1.3] - 2026-06-08

### 改进
- **space 身份重构**：以代码仓为身份中心统一坐标，含仓库转移重定向。

## [0.1.2] - 2026-06-06

### 新增
- `brain update`：从服务器拉最新客户端代码并重启常驻。
- `upload_folders` 变更后自动补全新范围的历史。

### 修复
- `npm link` 失败时自动配 PATH，不再 command not found。

## [0.1.1] - 2026-06-06

### 新增
- 只读查询原语 `grep` / `ls` / `log` + MCP 自带使用说明。
- 客户端健壮性增强 + 完整卸载；一键重接 MCP（Claude Code + Codex）。

## [0.1.0] - 2026-06-04

### 新增
- **团队大脑基线**：采集 Claude Code / Codex session → git 记忆库 → MCP 问答。
- `brain` CLI 一键安装 / 起停；端到端 onboarding（管理员一条命令加人、新人一键接入）。

### 安全
- 两轮安全审计：堵路径穿越、修并发归属等问题。
